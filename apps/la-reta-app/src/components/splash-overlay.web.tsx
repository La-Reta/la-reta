/**
 * En web no hay splash nativo que relevar: el documento ya está pintado
 * cuando monta React, así que un overlay solo añadiría un parpadeo.
 */
export function AnimatedSplashOverlay() {
  return null;
}
